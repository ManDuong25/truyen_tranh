# Harness durable layer — each project instance generates its own data.
harness.db
harness.db-wal
harness.db-shm

# Rust build output for harness tooling.
target/
dist/

# Downloaded Harness CLI binary for installed project instances.
scripts/bin/harness-cli
scripts/bin/harness-cli.exe

# Local brainstorming/visual companion sessions.
.superpowers/

# Local research/library clones and installed virtual environments.
arena-library/

# Local demo/runtime artifacts.
runs/

# Python local artifacts.
__pycache__/
*.pyc

# OpenCode local package cache; keep project agent configs under .opencode/agents.
.opencode/node_modules/
.opencode/package.json
.opencode/bun.lock
.opencode/.gitignore
